# Memory index

One line per memory. Filter here first; read bodies only after narrowing. See
`README.md` for the schema and recall rules. This public copy ships empty — real
memories live in the private backup.
